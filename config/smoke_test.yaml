# Lightweight configuration for CI / sanity check
# Runs on the tiny Cora dataset with just 1 epoch.

dataset: cora
epochs: 1
hidden_channels: 32
heads: 4
lr: 0.005
flop_budget: 0.1
quant_err_budget: 0.02
log_every: 1
